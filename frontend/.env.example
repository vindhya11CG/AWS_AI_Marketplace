# =============================================================================
# Frontend Environment Configuration (React + Cloudscape SPA)
# =============================================================================

# Option A: Live S3 Curated Catalog JSON URL (Direct S3 Public Read with CORS)
VITE_CATALOG_URL=https://ai-marketplace-624807913752-us-east-1-an.s3.amazonaws.com/curated/starter-packs.json

# Option B: CloudFront CDN Distribution URL (Recommended for production)
# VITE_CATALOG_URL=https://d111111abcdef8.cloudfront.net/curated/starter-packs.json

# AWS Lambda Function URL / API Gateway Endpoint
# Endpoints: /catalog, /starter-packs, /agents, /transform, /sync, /sync-graph, /items/{id}/comments, /items/{id}/ratings
VITE_API_ENDPOINT=https://yymryxj4se.execute-api.us-east-1.amazonaws.com/prod
