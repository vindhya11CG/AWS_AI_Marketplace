# AWS AI Marketplace - Frontend (Cloudscape)

Minimal Vite + React app scaffold using the Cloudscape Design System. This is a smoke-test frontend created on `feature/cloudscape`.

Commands:

```bash
cd frontend
npm install
npm run dev
```
